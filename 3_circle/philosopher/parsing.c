#include "philo.h"

static inline bool	is_space(char c)
{
	return((c >=   9 && c < = 13) || 32 == c) ;  
}

static cont char	*valid_input(cont char *str)
{
	int	len;
	const char	*number;

	len = 0;
	while(is)
}

static long	ft_atol(const char *str)
{
	long	num;
	num = 0;
	str = valid_input(str);

}

void	parse_input(t_table *table, char **av)
{
    table->philo_nbr = ft_atol(av[1]);    
}
